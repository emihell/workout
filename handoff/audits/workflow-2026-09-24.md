# Workflow-fit audit — 2026-09-24

**Scope.** The machinery, not product code. Read: `handoff/PLANNING.md`, `CLAUDE.md` (= `handoff/CLAUDE.md`,
symlink), `rules/{WORKFLOW,CLOSEOUT,AUDIT,DESIGN}.md`, `NOW.md`, `handoff/README.md`, `audits/workflow-2026-09-14.md`,
`plan` (save/publish/ping/next/sync/closeout), `scripts/check_handoff.py`, `scripts/capture.mjs`/`screenshot.mjs`
(headers), `.githooks/*`, `.claude/` in both worktrees + `~/.claude/settings.json`, `.github/workflows/deploy.yml`,
`log/DECISIONS.md` (digest + DEC-035/037/054..057/073..084), `log/LESSONS.md` (L-014..L-036), `BACKLOG.md` (head, Phase
2/3, tooling, reference tail), reqs 24/114/129/130/141/142/144(+prep)/150/152/153 + gate tags of req-120..153, reports
req-24/114/130/141/142/143/150/152/153, `SHIPPED.md` (last ~30 entries). **Method:** diagnosis only, nothing edited.
Unmarked claims have a receipt beside them; `[inferred]` = not run.

## Bottom line

The 09-14 audit's stale merge-gate spots were mostly fixed (req-70..73). What happened since is **volume and lane drift**.
Machinery is 3,211 lines, up from 2,393 (+34% in 10 days; `wc -l` over the 9 files the 09-14 audit counted).
`DECISIONS.md` is 1,448 lines and past its own ~1,000-line archive trigger. Its digest stops at DEC-045. 2026-09-24
merged **17 reqs** and made **87 `handoff/` commits** (`git log --since=2026-09-24 -- handoff | wc -l`), and every one of
those reqs went through the **same single process**. That process was designed for UI/logic changes to a
single-user localStorage app. Library content, design-only reqs, tooling and the coming backend all get the same Gate
tag (`functional`), the same report shape and the same closeout. So the gate misses what matters for each type. L-036 is
the case in point: the only net for view-flow regressions is a manual browser run, which a revert skipped. The biggest
future risk is the backend phase, and no rule covers it yet: secrets, ops, migrating real users off localStorage.

## Top 10 (impact / effort)

1. Scripted golden-flow browser smoke + `plan qa req-N` (isolated-origin branch build). Mechanises L-033/L-036. **H/M**
2. Refresh the DEC digest (it stops at DEC-045) and do the archive split. Fix the stale NOW/BACKLOG lines (E4–E6). **H/S**
3. Write the backend lane rules before any req-146 build: secrets, ops owner, a third ask-gate. **H/M [Emilio]**
4. Decide deploy cadence. Every closeout now ships to the live site Emilio uses in the gym. **H/S [Emilio]**
5. Migration receipt = a dry run on his real Export in an isolated origin, not only fixtures. **H/M [Emilio]**
6. Fix L-032 at the root. The hook text tells you to run `HANDOFF=1 git commit`, and `git commit:*` is granted. **H/S [grant: Emilio]**
7. Replace Gate tags with per-type lanes, each choosing its own gate checklist (§A). **M/M [Emilio]**
8. Move the reviewer check before the merge, as a refusal that needs a stated reason. Today it warns after the local merge. **M/S**
9. Collapse the three "batch" concepts into one lane rule. Resolve the "re-run vs don't re-run" contradiction. **M/S [lane: Emilio]**
10. Five skills: `qa-branch`, `review`, `spec`, `closeout`, `build-brief`. Only `audit` exists today. **M/M**

---

## A. Requirement types — one process forced on all

**Measured:** from req-130 to req-153, 14 reqs carry `**Gate: functional**`. The two exceptions are req-130
`persisted-data (additive)` and req-24 (a free-text Gate). The 14 are 10 library-content reqs, a progression-logic fix
(req-150), a removal (req-142) and 2 bug bundles (152/153). req-120..128 use `[P]` and
`[ux-feel]`. `[P]` is defined nowhere: `grep -n "\[P\]" handoff/rules/*.md handoff/PLANNING.md` finds nothing.
DEC-057 moved the safety triggers onto files. Since then the tag drives nothing, so it now only carries noise.

| Type | Readiness bar | Gate (who tests what) | Report | Emilio |
|---|---|---|---|---|
| **UI / frontend** | intent + constraints settled (WORKFLOW:118-131); behaviour calls marked `(unconfirmed)` | unit for logic; **Planner browser run of the branch build on an isolated origin** + golden-flow smoke; reviewer only if trigger files are touched; device feel after (non-blocking) | medium: test list ≤10 items | behaviour calls; feel after |
| **Bug fix** | a **reproduction** (command/browser steps + output, WORKFLOW:167-170) | test **fails on main, passes on branch** (both receipts); re-run the flow in a browser, including after any revert (L-036) | brief | only if the fix picks a behaviour |
| **Persisted schema / migration** | fully specified in advance (WORKFLOW:130); record counts stated | round-trip + legacy-key tests; reviewer mandatory; **dry run against his latest Export** (F3); backup reminder | full, with record counts before/after | **eyes before merge** (DEC-035 carve-out) |
| **Content / data (library)** | rules + validators + standing test sanctions (DEC-074); calls delegated (DEC-067) | validators + counts + main-chunk size (L-026/L-029) + spec review; the whole-library own-name scan (L-030); no browser beyond 10 real searches (L-027) | **table + counts only** | end-of-batch unconfirmed list |
| **Design / decision (no code)** | questions phrased with options (req-144:14-31) | each question answered by Emilio → `DEC-`; gap list → numbered reqs (req-144:47-51) | none (no branch) | always, live |
| **Backend / server** (new) | not defined; see F4 | integration tests against a local server; staging origin ≠ prod; restore drill | full | hosting, cost, security, privacy |
| **Tooling / workflow** | the failure it fixes, observed | self-test proves the guard **fires** (L-020, L-025); Planner uses the tool at once | brief | only if his steps change |
| **Audit** | brief + scope | none (read-only); findings → backlog | the report | reads it, triages |

**Mis-handled today:**
- **A1 Content reqs run the code gate** (M/S). Library batches got spec reviews (6 on 09-24, 9–13 findings each:
  `git log --since=2026-09-24 --format=%s -- handoff | grep "review ("`). They also got a Planner re-run of
  `node --test` and 80–270-line reports (req-141: 272, req-143: 240; `wc -l reports/*`). The spec reviews clearly earn
  their keep. The long prose reports don't: the receipts that matter are counts and validator output.
- **A2 Design reqs have no terminal state** (M/S). `classify_tag` knows merged/not-merged/blocked/unknown
  (`check_handoff.py:214-247`). A finished req-144 has no status to take: "BUILT AND MERGED" needs merge evidence, and
  anything else reads as `unknown`. req-146 and req-149 hold req numbers but have **no `work/` doc**
  (`ls handoff/work | grep -E '146|149'` → none). They exist only as BACKLOG bullets. → add a `DECIDED <date> → DEC-a..b,
  req-x..y` status that check_handoff accepts.
- **A3 The bug-fix lane lacks "re-run after revert"** (H/S). req-153's revert passed `./check` and slipped a Finish-exit
  regression that only Planner's browser run caught (L-036, SHIPPED req-153). With no automated flow test (F1), that
  catch was luck, not a guarantee.
- **A4 DEC-057 §1 over-fires the backup reminder** (L/S). It ties the backup reminder to `progress.js` and the other
  trigger files. req-150 touched `progress.js`, and its spec sensibly wrote "no backup needed (no stored-data change)",
  which departs from the rule as worded. → reviewer = code files; backup = only when stored records are written.
- **A5 Lane rule not followed** (M/S **[Emilio]**). DEC-055: "Batch (Emilio away, design settled) → one throwaway build
  agent per req". Yet all 16 SHIPPED entries for 09-24 say "Builder session", including 5 mechanical library batches.
  req-120..129 on 09-23 all say "throwaway agent" (`awk` over SHIPPED `Closeout` lines). Either DEC-055's condition
  ("Emilio away") didn't apply all day, or the rule and practice have split. Nothing records which.

## B. Automate

- **B1 `plan qa req-N [--seed file] [--port]`** (H/M). Do in one command what L-033 does by hand: `git archive <branch>`
  → scratch dir, symlink `node_modules`, `vite build`, serve on `127.0.0.1:<port>` (its own origin, no real data), print
  URL + `plan qa stop`. `capture.mjs` already builds two refs, serves them, and builds an active-workout seed through the
  UI (`scripts/capture.mjs:1-26`). Reuse that code. Today there are three ways to serve a build: `plan preview`
  (Tailscale, needs the code worktree), `capture.mjs`, and L-033's manual steps.
- **B2 Golden-flow smoke** (H/M). Puppeteer is already a dev dep (`screenshot.mjs:40`). Script the core flow: Start →
  log a set → Finish Save/Abandon → `#/`, device Back after Finish, an old workout URL, import of a bad file, and
  "0 native dialogs". Run it inside `plan qa`, and in `./check` if fast enough. It would have caught L-036 and req-152
  QA-1 without anyone thinking to look. `node --test` can't render JSX (L-007, L-010), so this is the only way to test
  views automatically.
- **B3 `plan gate req-N`** (M/S). Runs `./check` in an archive copy of the branch and prints one line formatted for
  SHIPPED's `Gate:` (DEC-057 §4). Closeout's SHIPPED stub (`plan:550-566`) fills it in. That ends the variants seen today:
  "Builder `check: green…`" (req-24/152/153), "Planner's own run → `# tests 844…`" (req-141), and
  "`# tests` all pass" with no count (req-142, which breaks DEC-057 §4).
- **B4 Reviewer check before the merge, blocking** (M/S). `cmd_closeout` merges at `plan:742` and calls
  `reviewer_warning` at `plan:801`, after the local merge, and never blocks. DEC-057 says "the reviewer comes BEFORE
  merge". → check before the `git merge`; refuse unless the report names a reviewer or closeout gets
  `--no-reviewer "why"`. The reason goes into SHIPPED.
- **B5 Session-start fetch** (M/S). L-014 made `git fetch` mandatory, but no `plan` subcommand fetches (`grep -n fetch
  plan` → nothing). PLANNING.md:132-137 is a manual 4-command block. → `plan status --fetch` (or `plan start`): fetch,
  report origin-ahead / unsaved / unpublished, and exit non-zero if origin is ahead.
- **B6 Drop the manual publish-gate block** (L/S). PLANNING.md:162-178 repeats checks `plan publish`/`plan next` already
  make (`plan:367-417`). Keep one line: "`plan publish` refuses unless the code worktree is on a clean main".
- **B7 Widen check_handoff** (M/M). It scans req Status lines, NOW's forward regions, and size claims (`check_handoff.py:1-30`).
  It exited 0 today (`python3 scripts/check_handoff.py --repo .; echo $?` → `0`) while E4–E6 were stale. Worth adding:
  (a) BACKLOG lines calling a merged req "READY/in flight"; (b) the digest's highest DEC against the last DEC;
  (c) any `workout-mvp-v<N>` in a live-rule context that isn't `STORAGE_KEY` (L-018's grep, made permanent).
- **B8 Throwaway-agent setup** (L/S). The `node_modules` symlink, "no merge/push" and the report shape are repeated in
  every dispatch prompt [inferred: no template file exists]. → a `build-brief` skill (C5) or `plan brief req-N`, like `plan ping`.

## C. Skills

**Exist:** only `audit` (`.claude/skills/audit/SKILL.md`, same in both worktrees; `ls -R .claude/skills`). Every other
repeated procedure is prose in PLANNING/WORKFLOW/CLOSEOUT, re-read and re-typed each time.

- **C1 `qa-branch` (Planner)** (H/M): `plan qa` → golden flows → the req's own test list → "0 native dialogs" check →
  restore/throw away the seed → a receipt block for SHIPPED. Includes L-033's gotchas (375 px iframe, the 10 s countdown
  inside one script, stop Back at the iframe's first entry, from req-153's report).
- **C2 `review` (Planner-spawned subagent)** (M/S): one brief for spec review and one for diff review. Cold diff
  (`git diff main..req-N`), the DECs its nouns touch (WORKFLOW READY check 1), output as blocker/should-fix/nit, and
  **"latent" findings listed for re-check if the code that hides them changes** (L-036). Also the L-031 guard: a
  history-free reviewer's proposals pass a before-check. Used about 12× on 09-24 [inferred from SHIPPED + commit subjects].
- **C3 `spec` (Planner)** (M/S): a template per lane from §A, with READY checks 1–6 (WORKFLOW:133-170) as prompts. Covers
  trigger files, siblings, failure criterion, `(unconfirmed)` marks, and the reproduction for bugs.
- **C4 `closeout` (Planner)** (M/S): recording pass → `plan gate` → `plan closeout` → NOW prune → publish receipt.
  CLOSEOUT.md becomes the skill body (it is already "the ONE process… identical every time", CLOSEOUT.md:3).
- **C5 `build-brief` (for Builder and throwaway agents)** (M/S): branch naming, `./check`, report shape by lane, "never
  merge", browser on an isolated origin only (F8).
- **C6 `workflow-audit`** (L/S): this audit is ad hoc each time. AUDIT.md:7-12 describes it without any procedure.
  Mark it `disable-model-invocation: true` like `audit`.

## D. Improve / remove

- **D1 Volume** (H/M). The files the 09-14 audit counted: 2,393 → 3,211 lines. DECISIONS 937 → 1,448; LESSONS 172 → 409;
  WORKFLOW 291 → 340; PLANNING 442 → 457. DECISIONS.md:8-10 says to split to `DECISIONS-archive.md` "~1000+ lines". It
  hasn't been done. → do the split (a move, not a rewrite). Treat the digest as the only read path and keep it current
  in the same commit as each new operational DEC.
- **D2 Three overlapping "batch" concepts** (M/S). DEC-035 batch/single (PLANNING:262-266), DEC-047 "batch mode — the
  opt-in exception" with relaxed testing (WORKFLOW:242-269), and DEC-055 lanes (PLANNING:206-220). WORKFLOW:244-245 says
  "The default stays: one req at a time, human-gate testing per req (DEC-035). Do not loosen it." That contradicts DEC-035
  itself (planning merges on its own testing; no human gate). → one rule: **lane** = live-with-Emilio | Builder-single |
  throwaway-batch. Testing is the same in every lane (§A per type). The only thing a batch changes is "don't stop between
  reqs".
- **D3 Re-run contradiction** (M/S). PLANNING:248-250 says "don't trust CC's pasted `./check`", and DEC-035 says "planning
  re-runs, never trusts the receipt". PLANNING:445-457 says "Don't re-run a green suite… read the report". Practice
  splits the same way (B3). → once `plan gate` makes the re-run a single command, always re-run and delete :445-457's
  mechanical bullet.
- **D4 `/clear` ceremony** (L/S **[Emilio]**). PLANNING:268-274 tells Planner to remind Emilio at every single-mode close.
  16 reqs went through one Builder session on 09-24 (A5). Either it's done and not recorded, or it's no longer done.
  → tie it to Builder's context use, not to each req.
- **D5 Legacy paste path** (L/S). PLANNING:296-309 (the "four parts… still used when Emilio pastes") and :338-344
  (verifying a pasted prompt) predate SendMessage + `plan ping`. → drop them, or reduce each to a line.
- **D6 Gate tag vocabulary** (M/S). `functional/ux-feel/persisted-data/infra` + undocumented `[P]` no longer select any
  check (A). → replace with the type tags from §A. check_handoff doesn't parse Gate, so this is a docs-only change.
- **D7 Report + SHIPPED size** (L/S). CLAUDE.md says "Size the report to the req". req-142 (a mechanical removal) had an
  83-line report; library batches 99–272. SHIPPED is 1,820 lines, and its entries now run 10–15 dense lines that repeat
  the report (SHIPPED req-24/152/153). → SHIPPED: 3 lines (what, gate receipt, report path). Reports: caps per lane.
- **D8 CLAUDE.md length** (L/S). It is 235 lines, loaded on every Builder turn, and calls itself short ("so it stays short",
  :13). The merge model is stated in both "How work arrives" (:105-149) and "You report it ready" (:203-225). ~20 lines trimmable.

## E. Fix / correct (stale or wrong now)

- **E1** `CLAUDE.md:59`: "`Edit(handoff/**)` catches some of it earlier". **No such deny exists** in the planning
  `.claude/settings.json`, the codebase `.claude/settings.json`, or `~/.claude/settings.json` (`cat` of all three: deny =
  only `.claude/settings*.json` + `.claude/hooks/**`). `handoff/README.md:31-32` makes the same claim: "a permission deny
  rule blocks the Edit tool". Either add the deny (settings are Emilio's) or delete both claims.
- **E2** `CLAUDE.md:21` "Ephemeral build agents **you** spawn report back to Planner", and `handoff/README.md:24-25`
  "Ephemeral build agents **Builder** spawns". Under DEC-055, **Planner** spawns them.
- **E3** `.githooks/pre-commit:3` "Emilio lands planning changes with: HANDOFF=1 git commit ..." and `:16-17`
  "If this is intentional planning work: HANDOFF=1 git commit ...". This is the exact path L-032 forbids, and following
  the hook's own advice is what tripped auto mode. → point it at `./plan save "msg"`. The planning
  `settings.local.json` also grants `Bash(git commit:*)` and `Bash(git reset:*)`.
- **E4** `DECISIONS.md:37` digest: "never overwrite an unreadable `workout-mvp-v8` key". The live key is v9
  (`src/storage.js:6`; `reference/schema.md:11`). Also at `:31-33`: "Build lanes → DEC-037… No `/clear`" leaves out
  DEC-054/055 and conflicts with PLANNING:268 (`/clear` between single reqs). The digest names nothing after DEC-045,
  which leaves 39 DECs (046–084) off the read path.
- **E5** `NOW.md:32` "**Backend fork** (gates Phase 3): browser-only vs database/server (+users, AI key). Decide first."
  DEC-073/075 decided it: "it runs on a computer Emilio uses as a server". NOW.md:22 itself says so. `BACKLOG.md:548-551`
  "Decide the target before starting any of these" is stale the same way.
- **E6** `BACKLOG.md` Phase 1 head: `:37-38` "`req-01` guard `saveState` (in flight, READY)" (shipped: NOW.md:3);
  `:40-41` "The app currently has **zero CSS**"; `:590-595` "What's missing is only the auto-backup-before-import net"
  (req-07/115 shipped). `:597-612` Current shape: "storage.js … v5–v8 migrations", "views/ … (no CSS yet)", and only
  `exerciseCatalog/Extras`. `src/exerciseLibrary.js` and `src/library/` exist (`ls src`). `CLAUDE.md:89` says "BACKLOG.md
  is an **index**, not the backlog". It is a 612-line backlog with an index at its head.
- **E7** `BACKLOG.md:560` (AI generation): "Falls under CLAUDE.md's external-call ask-gate". **No such gate exists.**
  CLAUDE.md has two ask cases (:184-202), and "irreversible external call" appears only as an analogy (:198). This
  matters as soon as a server or an AI key arrives (F4).
- **E8** `WORKFLOW.md:259-261`: "With no browser in either session, the pre-merge visibility check is a scripted
  screenshot… or Emilio's fast eyeball". Planner browser-tests now (L-033; SHIPPED req-24/152/153 browser receipts).
  Also `WORKFLOW.md:244` (D2).
- **E9** `PLANNING.md:153` "to avoid a needless redeploy (until req-04)". req-04 shipped, and `deploy.yml:4-13` already
  filters by path. `PLANNING.md:220` "Functional single reqs → … Emilio at the gates" contradicts DEC-035 (also in
  DECISIONS:788, the archive, fine).
- **E10** `CLOSEOUT.md:11` "CC reports the branch built + human-verified", which predates DEC-035. `:13-15` "`closeout`
  does NOT record" is half-stale: it appends a SHIPPED stub, bumps NOW's Shipped range and prints the reviewer warning
  (`plan:550-632`, req-90/129). The step list at `:23-28` leaves all three out.
- **E11** `CLAUDE.md:212-213`, report item 3: "anything needing a browser" is listed as something Builder can't verify.
  Builder does browser runs (reports/req-153.md Workflow: "My browser runs twice walked the tab off the app"). → say
  "a real device".
- **E12** `DESIGN.md:10-13` "A logging tool for one person's real training… not a coach, a social app", and
  `README.md:118-120` "No accounts, sharing, collaboration". DEC-073 says "other users soon… workout together". This is
  contract drift, and reconciling it is Emilio's call (AUDIT.md:21-25).
- **E13** `plan:669,674,693` comments attribute branch-slug resolution to "(req-146)". req-146 is now the backend design
  (BACKLOG:533). A number collision: the code history points at the wrong req. (L)
- **E14** `WORKFLOW.md:77-78` "Split into a folder only when it passes ~200 lines". `req-144-prep.md` (660 lines) and
  `req-140-parity-queue.md` are sibling files, not folders. (L; decide whether the rule or the practice wins.)

## F. Holes and future risks

- **F1 No automated net for view flows** (H). `node --test` has no JSX transform (L-007, L-010). Every navigation
  regression so far was caught by a human or Planner in a browser: QA-1..4 (req-152), L-036 (req-153), the invisible
  button (L-024/req-88). The more that ships per day, the more depends on someone remembering to look. → B2.
- **F2 Every merge deploys to the device he trains on** (H **[Emilio]**). `deploy.yml:3-13` deploys on any `src/**` push
  to main. `plan closeout` pushes main (`plan:810`). On 09-24 that meant 17 req merges, so up to 17 live deploys during
  a day he might train [inferred: some content-only]. Device feel is explicitly caught "in real use" (NOW.md:5). A
  regression mid-workout lands on his real history. Options: a daily/explicit release (closeout pushes a `release`
  branch Pages builds from), or B2 as a required deploy gate in CI.
- **F3 Migrations are proven on fixtures, not his data** (H **[Emilio]**). DEC-035 says it outright: "a unit test proves
  the code does what the test says, not that the test covers your real (messy) `localStorage`". Today the only answer is
  "Emilio's eyes". → the carve-out's receipt becomes: load his latest Export into the isolated `plan qa` origin, run the
  migration, and diff counts per entity before/after, plus a spot-read of 3 workouts. The export stays in the scratchpad,
  never in git (DEC-068 pattern). He has to hand over an export for this.
- **F4 The backend phase has no rules** (H **[Emilio]**). `.gitignore` has no `.env`/secrets entry (only `*.local`).
  There is no secret-scanning hook. The DEC-005 isolation model (planning never touches code) has no owner for **ops**:
  a server on his own computer serving other people needs uptime, patching, TLS, backups and a restore drill (BACKLOG:533-541
  raises these as questions only). The ask-gates cover localStorage only (CLAUDE.md:184-202). Moving every user off
  localStorage is the largest bulk write this app will ever do, and it runs on devices Planner can't reach. CI only
  deploys Pages; `./check` has no server tests. GDPR applies to other people's health data. **Before the first backend
  build req:** a `rules/BACKEND.md` (or DECs) covering a third ask-gate (deploy, DB migration, anything that reaches
  another person), secrets out of git + a pre-commit scan, a staging origin, the ops owner, and restore-from-backup as an
  acceptance criterion.
- **F5 Multi-user and live group workouts** (M). The seeds, `db.json`, the test tools and the "one person" design values
  (E12) all assume one user. Shared routines plus live sync mean concurrency, which none of the current tools
  (node tests, a single-origin browser run) can exercise. Flag in req-146. Plan a two-client test harness with the server.
- **F6 Permission-mode friction** (M/S **[Emilio]**). Planner runs `defaultMode: "auto"` (`~/.claude/settings.json`).
  Cross-session messages to Builder were held for approval three times on 09-24, so Builder was launched in a
  different mode [inferred: no in-repo setting differs]. Each hold stops the loop until Emilio returns. → start both
  sessions in the same mode, or add a project-level allow for cross-session messaging. [inferred: not verified that
  SendMessage is gated by a configurable rule; check with claude-code-guide.]
- **F7 The reviewer is only a reminder** (M). B4. And once reviewed, a "latent" finding goes nowhere
  (L-036: the reviewer flagged the Finish redirect as latent, and the revert made it live).
- **F8 Builder browser runs use the shared dev origin** (M/S). reports/req-153.md: "the dev data was left with an active
  workout… Byte-identity with the earlier state isn't proven for that one run". L-033's isolated-origin method is
  Planner-only. Builder has no equivalent rule, and L-001 was the same class. → `plan qa` for both sessions.
- **F9 Planner is a single long-lived point of failure** (M). One session wrote 17 specs, ~12 review briefs, the
  browser runs, every merge and 25 DECs (DEC-060..084) on 09-24 alone. L-035 (a DEC reason that was never read) and the stale lines
  in E4–E6 are the kind of error long context produces [inferred link]. → rotate the Planner session at a milestone or
  every ~N reqs, with NOW.md as the handover (it's built for that).
- **F10 Subagent cost is unmeasured** (L/S). 09-24 used ≥6 spec reviews, several diff reviews, 4 RepDB-blind scan agents
  (req-141) and a prep agent; 09-23 used 5 flow-audit reviewers. The value is evidenced (9–13 findings per spec review;
  L-031's 6 caught reverts). Cost is recorded nowhere. → one line per SHIPPED entry: agents spawned.
- **F11 Milestone vs work** (WORTH). NOW.md:11 says "Nothing heavy before the flow is flawless". On 09-24, 12 of 17
  merges were exercise-library work, a Phase 3 item pulled forward by DEC-060. That was decided, but NOW's milestone
  line doesn't say so. A cold reader would take the day's work for drift.

## Decisions for Emilio

1. **Lanes (A5, D2, D4):** does DEC-055 still hold? Should throwaway agents run batches, or the Builder session? Keep the
   `/clear`-per-req reminder, or tie it to context use?
2. **Deploy cadence (F2):** keep deploying on every merge, or move to an explicit release step / smoke-gated deploy?
3. **Real-data migration dry run (F3):** will you provide a fresh Export for Planner to dry-run migrations on (scratchpad
   only, never git)?
4. **Builder permission mode (F6):** same mode as Planner, or an explicit allow for cross-session messages?
5. **Per-type lanes (A, D6):** adopt the §A table in place of the Gate tags?
6. **Backend rules before req-146 builds (F4):** a third ask-gate (deploys, server migrations, anything reaching other
   users), secrets policy, and who owns server ops.
7. **Product contract (E12):** when do README "Deferred scope" and DESIGN §0 change to reflect DEC-073?
8. **Grants (E3, E1):** remove `git commit:*` / `git reset:*` from planning's machine-local grants. Add
   `Edit(handoff/**)`/`Write(handoff/**)` denies in the code worktree, or drop the claim.
9. **DEC-057 §1 split (A4):** backup reminder only when stored records are written?
10. **Size caps (D7):** SHIPPED at 3 lines per req; report caps per lane.

## Coverage

Done: every doc in the brief. `plan` read in the save/publish/ping/next/sync/closeout paths; preview/doctor only by
their usage text. check_handoff read in structure + status classification, and run (exit 0). Hooks read in full.
Settings in both worktrees + user-level. Cadence from `git log` in both worktrees. Sampled reqs + reports as listed.
Partial: DESIGN.md (sections 0 + values only); SHIPPED (last ~30 entries); BACKLOG (head, Phase 2/3, tooling,
reference). Not done: running `./check` or the self-tests (they write `dist/`, and this pass is read-only); how long a
cross-session approval hold lasts; subagent token cost.
Confirms existing (not counted as new): BACKLOG's "Slim the readiness taxonomy" (overlaps D6); DEC-084's parked dead-Back
idea; the "per-branch previews" folded into req-146 (overlaps B1 for the pre-server period).
