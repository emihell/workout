# Inbox — raw asks, saved exactly as Emilio wrote them

**Rule: Emilio's words go here byte-for-byte — no spelling fixes, no rewording, no
inline annotations, no reordering.** This file is the untouched record. Planner
grounding, dispositions, and req-mapping live elsewhere (a processing doc or the
req files themselves), never mixed into the text below. An item is cleared from here
only once it is specced as a req, and even then the raw text is preserved in history.

---

## Batch: workout / gym-flow notes

Sent by Emilio in an earlier session, lost before capture, re-sent and saved
2026-09-16. Reproduced exactly as sent:

```
Workout

If nothing is Scheduled today, make start button ”start new workout”

Time for times excercises - already a req for this?

If i start a exercise in a workout, go elsewhere in the app, then press continiue on the active workout - i should go straight in the exercise

There should maybe be a floating button for active workout? Or a signal in the workout button in menu?

When i finish last set, i see the rest timer in the exercise menu, make the rest timer much much smaller, in a little pill/button - föoating in absolute - maybe remove the timer screen and move it into each following page - similar to above,  when i do one set, and press complete, i move to next set but have the rest pill/button (whatever is best) om it, that way we remove a button press, a extra page, and make it simpler?

In the exercise list for a workout that is active, make a bigger visual difference between excersises that are done and those that are not done, so your eye focuses on the ones that are not done, while the done are less visible (as they are done and will probably not be interactwd with anymore)


During an exercise
Previous skip next should be place at the absolute bottom of the page, and add note and be something small beside the exercise title

Maybe have a new button only for development, that is a button where i can add notes to a page - so when i am doing an exercise - and see a flaw or improvment possavility - i can press this little (very little and unimposing) button and add a note - it's saved connected to that page - so you can see what its connected to and see my notes and create a req from it

If i change a set setting during workout - that should be saved for the future - for example, i set 4kg to 5kg during wars up - that should now the default for all future workouts

When all exercise in a rutine is done, auto complete? Like a great job, finishing rutine in 10 sek showing stats/improvments. From last time- then auto complete the whole rutine but have option to edit (ask user to intervene if needed, but goal should be that everything is automated and user interaction is minimal) - instead of having a list all done and press done


On main page - remove the complete today - it becomes a duplicate? Maybe the list on the main page should be refactored, and become one components? I am guessing its several components right now and thats why we introduce these weird states?
```
