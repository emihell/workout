# req-113 — routine editor shows "—", not "0", for a set with no weight

## Technical

- `src/ids.js` `routineItemMeta`: when the kg part prints (some weight > 0, unchanged gate), each weight that is not > 0 (`0`, `''`, `null`/missing) renders as `—`. `Array.from` so a sparse-array hole also maps to `—`.
- `src/ids.test.js`: new `req-113` suite — `[0, 40]` → `—/40 kg`, `[40, 0, 45]` → `40/—/45 kg`, `['', 40]` and `[null, 40]` → `—/40 kg`; `[0, 0]` and `[]` → `2 sets`. req-103 tests unmodified and passing.
- `./check`: `check: green — lint, 27 test file(s), and the build all passed.` [measured]
- `node --test src/ids.test.js`: `ok 5 - req-113 routineItemMeta shows — for a set with no weight` (2/2 subtests), `# pass 14  # fail 0`. [measured]
- Screenshot (`npm run shot -- /routines/sess-upper --seed <db.json with Chest Press suggestedWeights [0, 40]> --build --out screenshots/req-113.png`): Chest Press row reads `WU set · 2 sets · —/40 kg`. [measured]

## Workflow

- No deviation. Spec-open choice: negative weights (not in the spec) also count as "no weight" and show `—`, since the gate is `Number(w) > 0`.
