# req-163 — "last time" comparisons and History set bugs

Branch `req-163` (`3624c7f`, then this report). Lane: bug. The reviewer fires (`storage.js`). No stored data is
rewritten.

## Technical

| # | on main | change |
|---|---|---|
| 1 F-CODE-3 | **bug**: `previousSameRoutineWorkouts` used array order | new `finishedNewestFirst(workouts)` (`storage.js`): finished only, `finishedAt` desc, then `id` desc. Both `previousSameRoutineWorkouts` (Finish's beat-last-time, auto-complete's prior) and `lastSetsForExercise` (prefill, prescription) read it. The grep for other "previous/last" pickers found only display lists, which already sort by date (`sortWorkoutsByDate`), plus id lookups. |
| 2 req-109 b | **already fixed** by req-128 (`historyPrescription` prefers the non-`addedMidWorkout` item). beat-last-time's first-item lookup reads only name/type, never rest. | none; pinned by a test |
| 3 req-111 a | **partly**: req-128 skipped all-skipped priors (`anythingLogged`), but a **warm-up-only** prior still counted | `summaryPriorWorkout` needs a done working set (`hasDoneWorkingSet`, DEC-053). The unused `anythingLogged` import is dropped from storage.js. |
| 4 req-111 b | **already correct** since req-152: warm-up-only → `historyHasSetAt(work)` false → the DEC-002 carry applies. The only other seed caller (`setPreview`) runs with no carry, and `historyPrescription` returns null (no work history). All paths agree. | none; pinned by tests composed exactly as item.jsx composes the seed |
| 5 req-117 b | **bug**: History's set form had no Duration | `SetEditForm` takes `showDuration` (a boolean, or a function of the form's set type) and adds a `Duration (s)` field parsed by `seconds-input.js` (`45,5` → 46, `abc` refused inline). `historySetFields` saves `durationSec` (blank → null, and no key when the field isn't shown). `withHistorySet` carries it. It shows on a timed exercise's **work** set only, as the live form does. |
| 6 DEC-087 §2 | **bug** | (a) `formatSetLine(set, { cardio })` never labels effort on a WU or cardio set. It's display only, and the stored rpe stays. (b) History Edit/Add pass `showEffort` from `historySetKind` (`set-values.js`): hidden on WU/cardio, following the WU/Work toggle live. A hidden Effort saves `rpe: null`. The kind comes from the snapshot item (frozen at Start), else the exercise record. |

### Receipts

**Red on main.** `src/req-163.test.js` was run against `git archive main` in a temp dir with the test file copied in
(L-040). Result: 15 fail / 5 pass. The 5 that pass are the 2 and 4 pins, plus item 5's negative case ("no Duration where
it doesn't belong"):
```
    not ok 1 - an oldest-first array → Finish's comparison and the prefill pick the same, newest workout
    not ok 2 - the same answer whatever the stored order
    not ok 3 - equal finishedAt → a stable tie-break (id), identical for both pickers and both array orders
    not ok 4 - the auto-complete prior (summaryPriorWorkout) follows the same order
    ok 1 - a replacement item BEFORE the own item → the own item rest and notes
    not ok 1 - a warm-up-only prior is passed over for the older one with work (was: the warm-up-only one)
    not ok 2 - only warm-up-only / all-skipped priors → null (no invented deltas)
    ok 1 - the first work set has no invented kg; the warm-up still prefills from its own history
    ok 2 - the next work set gets the DEC-002 carry of the kg just logged
    ok 3 - every path agrees: the prescription (setup / replacement) sees no work history either
    not ok 1 - Edit: the Duration (s) field shows the stored seconds; "45,5" saves durationSec 46
    not ok 2 - Add set: a timed exercise gets Duration, and it is saved
    not ok 3 - unreadable seconds are refused inline; nothing written
    ok 4 - a non-timed exercise has no Duration field and writes no durationSec key; a WU set of a timed one neither
    not ok 1 - (a) an OLD warm-up set stored with rpe 3 shows no "Moderate"; its stored rpe is still 3 (no write)
    not ok 2 - (a) an old cardio work set with rpe 3 shows no "Moderate" either
    not ok 3 - (a) formatSetLine: WU / cardio never labelled; a work set is
    not ok 4 - (b) editing a WU set: no Effort control, and Save writes rpe null
    not ok 5 - (b) toggling the type to Work brings Effort back; to WU hides it again
    not ok 6 - (b) a cardio work set: no Effort, rpe null; a normal work set keeps Effort and its value
# pass 5
# fail 15
```
On the branch it's `# pass 20 # fail 0`.

**Browser** (`./plan qa <ref> --seed req163-seed.json`, then a scratchpad puppeteer script at 390 px, clock at Mon
2026-09-21). The seed is db.json with:
- `workouts` **reversed** (oldest-first, like a non-newest-first import);
- Plank `hasDuration: true`;
- w42's Chest Press WU set given `rpe: 3` (the pre-req-156 shape);
- w42's Chest Press snapshot `restSec: 45` (the routine's is 90).

req-163:
```
seed workouts order (stored): wo-w38, wo-w39, wo-w40, wo-w41, wo-w42
[5] Plank set edit, fields: kg, Reps, Duration (s), Note
[5] stored Plank set: {"reps":"60 sec","durationSec":46}                  ← typed "45,5"
[6a] Chest Press rows: ["WU set · 10 kg · 12","30 kg · 12 · Hard","35 kg · 11 · Failure","35 kg · 9 · Failure — failure at rep 7"]
[6a] stored WU rpe (display only, not rewritten): 3
[6b] WU set edit shows Effort? false | controls: kg, Reps, Note
[6b] stored WU rpe after Save: null
[1] Chest Press preview (prefill = lastSetsForExercise): WU · 10 kg × 12 | 1 · 30 kg × 12 | 2 · 35 kg × 11 | 3 · 35 kg × 9
    logged Chest Press sets: ["wu 10x12","work 32x8"]
[1] Finish screen beat line: (none)                                       ← 32 vs the NEWEST prior (w42, best 35)
[2] live snapshot, Chest Press items in order: [{"restSec":45,"addedMidWorkout":true},{"restSec":90,"addedMidWorkout":false}]
[2] new replacement restSec (historyPrescription of last time): 90 (own item 90, replacement 45)
native dialogs: []
```
main, the same script:
```
[5] Plank set edit, fields: kg, Reps, Note                                ← no Duration (the script stops here)
[1] Finish screen beat line: ↑Heavier on Chest Press                      ← 32 vs the OLDEST (w38, best 30), from an earlier run of the same script on main
[2] new replacement restSec (historyPrescription of last time): 90        ← same as the branch: fixed by req-128
```
The prefill line is identical on both builds (w42's weights). So on main the Finish screen and the log form disagreed
about which workout was "last time", and on the branch they agree.

`./check --smoke`:
```
smoke: green — 11 steps (build 0.4s, browser 4.5s)
check: green — lint, skills, 65 test file(s), the build, and the smoke test all passed.
```

### Test edits (why each)
- `storage.test.js`, req-84 "most recent finished workout" and req-111 "every prior newest-first". The fixtures had no
  `finishedAt` and relied on array order, which is exactly what item 1 removes. They now carry `finishedAt` in the same
  newest-first order, and the assertions are unchanged.
- `req-154.test.js`: the SetEditForm source pin now allows the Duration check in the save gate (`|| secondsProblem`), the
  same edit req-155 made to its SetLogForm twin. It still requires the kg check first and a return before `onSave`.

## Workflow

- **Stale spec, the code wins:** items 2 and 4 were already correct on main (req-128 and req-152 landed them; the
  BACKLOG lines weren't struck). Item 3 was half done. I didn't invent changes for 2 and 4; I pinned them with tests
  (receipts above). BACKLOG §req-109 (b) and §req-111 (a)/(b) should be marked done.
- **Choice the spec left open:** Duration shows on a timed exercise's **work** set only, as the live log does. The Reps
  field stays, so an old free-text "60 sec" set can still be edited.
- **Seen, not fixed:** an Add-set snapshot item ("Added during history correction") carries no `hasDuration`. The kind
  falls back to the exercise record, so it works, but the item's shape differs from Start's.
- My browser script's first runs misfired twice. Triple-click didn't select a prefilled kg, and at 390 px the fixed dock
  covered the lowest row, so the click hit "Workout". Both were script bugs, fixed by `input.select()` and
  scroll-to-centre. For the qa-branch skill: centre-scroll before clicking at phone width.
