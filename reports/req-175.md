# req-175 — plain words in the workout and History

Branch `req-175` (off `7dc75ff`). Display text only. No stored value changes.

## Technical

**Sites** (`grep -rn "WU\|Failure\|Replace exercise" src scripts`, plus the volume `kg` sites):

| site | old → new | |
|---|---|---|
| `ids.js:32,39` RPE_OPTIONS / rpeLabel | Failure → Couldn't finish | changed |
| `ids.js:121` formatSetLine (workout item + History rows) | WU set → Warm-up set | changed |
| `workout-log.js:720` set-preview label | `WU ·` → `Warm-up ·` | changed |
| `workout/item.jsx:424` title bit, `:506` link (+ comment :489) | WU set / Replace exercise → Warm-up set / Swap exercise | changed |
| `workout/overview.jsx:75` | · WU set → · Warm-up set | changed |
| `workout/replace.jsx:59` title, `:68` row button | Replace exercise / Replace → Swap exercise / Swap | changed (button: see Workflow) |
| `history/detail.jsx:55` volume | `{n} kg` → `{n.toLocaleString('en-US')} kg lifted` | changed |
| `history/detail.jsx:131`, `history/helpers.js:21`, `history/edit.jsx:152,196` | WU set → Warm-up set | changed |
| `ui/index.jsx:401` SetLogForm default effortOptions, `ui/Showcase.jsx:30` | Failure → Couldn't finish | changed (same list, consistency) |
| `workout/auto-complete.jsx:76` | `Volume` row, already labelled | left (spec: only if unlabelled) |
| `ids.js:95` routineItemMeta (routine editor row only), `Routine.jsx:351` checkbox | WU set | left, routine form → req-144; separate literals, not shared |
| `ids.js:63` ROUTINE_ROLES `WU routine` | — | left, not in the table (see Workflow: it *is* shared) |
| `exchange.js:19,38,40,41` | WU | left, assistant prompt text, not on screen |
| `db.json` `note: "Failure"` | — | left, seed user data |
| `replace.jsx:47` `recordButton('replace-exercise')`, component/route names | — | left, ids not text |

**Receipts**

- `node --test src/req-175.test.js` → `# pass 2  # fail 0`. (1) live log screen over the real store: Warm-up set / `Warm-up · ` / Swap exercise on screen, none of the old strings; picks Effort value 5 ("Couldn't finish") and finishes; the four finished Leg Extension records deep-equal the ones printed by the same steps run against a `git archive` of origin/main (wu rpe null; work rpe 5; two skips). (2) db.json (v8 backup) through `store.applyBackup`: stored setType/rpe kept, History exercise shows `Warm-up set · ` and `Couldn't finish`, detail shows `5,873 kg lifted`. Run against the main export, both fail (`did not match /Warm-up set/`), so they test the change.
- Browser, puppeteer 390x844, dialogs stubbed (`dialogs: []` both runs), `node scripts/qa.mjs start <ref>`, default seed:

| screen | main 7dc75ff | req-175 a8fd40d |
|---|---|---|
| History detail | `… 28 sets · 5873 kg`, `Chest Press — WU set · 4 sets` | `… 28 sets · 5,873 kg lifted`, `Chest Press — Warm-up set · 4 sets` |
| History exercise | `WU set · 10 kg · 12`, `35 kg · 11 · Failure` | `Warm-up set · 10 kg · 12`, `35 kg · 11 · Couldn't finish` |
| Overview preview | `Chest Press · WU set` | `Chest Press · Warm-up set` |
| Item log | `WU set · 1/4`, `WU · 10 kg × 12`, `Replace exercise` | `Warm-up set · 1/4`, `Warm-up · 10 kg × 12`, `Swap exercise` |
| Effort segments | Easy, Moderate, Hard, Failure | Easy, Moderate, Hard, Couldn't finish (no overflow: scrollWidth ≤ clientWidth) |
| Swap screen | `Replace exercise`, `Replace` | `Swap exercise`, `Swap` |
| stored after WU + value-5 set | `[{wu,null},{work,5}]` | `[{wu,null},{work,5}]` |

- `./check --smoke` → `check: green — lint, skills, no import cycles, 76 test file(s), the build, and the smoke test all passed.`

**Test edits** (old text → new; the input/steps are unchanged): `ids.test.js:21,27` (Failure); `req-128.test.js:20,22`, `req-152.test.js:92`, `req-153.test.js:274` (historyGroupMeta WU set); `req-156.test.js:97` (fixture option label) and `:126` (assert /Warm-up set/); `req-163.test.js:186,199,215`; `req-167.test.js:101,114` (toggle button label); `workout-log.test.js:839,871,889,915,934` (`WU ·` → `Warm-up ·`); `scripts/smoke.mjs:239,240,247` (waitText `— Warm-up set · …`). `ids.test.js:66` (routineItemMeta `WU set`) is unchanged: that code was left alone.

## Workflow

- Left open by the spec, my choices: the picker's per-row **Replace** button → **Swap** (same screen, same action). The separator is `toLocaleString('en-US')` (a comma whatever the locale; the rest of the app is English). The finish summary is unchanged because its row is already labelled "Volume". The default effort list in `ui/index.jsx` and the Showcase list were changed to match.
- Shared-string rule: no **WU set** string is shared with the routine form (the checkbox and `routineItemMeta` are their own literals), so the routine editor still reads "WU set" until req-144. **`WU routine`** (`ROUTINE_ROLES`, via `roleTag`) *is* one string shared by the routine form, History (`Rowing — WU routine · 1 set`) and the workout overview (`Rowing WU routine`). It isn't one of the four rows, so I left it. The result is that "Warm-up set" and "WU routine" sit side by side on the same screens. req-144 should take this, or Planner can widen this req.
