# req-170 — a workout's routine falls back to the one its snapshot names

Branch `req-170`. Data lane: reviewer + Emilio before merge (DEC-090).

## Technical

**Change:** `src/model.js:88` (`workoutSnapshot`):
`routineId = workout.routineId || workout.sessionId || workout.snapshot?.routineId || workout.snapshot?.sessionId`.
Top-level still wins. No field removed from any record; no schema bump (still v9).

**Dependents checked:** `workoutSnapshot` is called for finished, draft and active workouts (`model.js:330,333,336`).
Its `routineId` feeds `snapshot.routineId`, `foundRoutine` (template match, routine-name fallback) and
`programLabelFrom`. For data the app wrote, top-level `routineId` is always set, so none of these change (golden below).

**Criteria**

1. Migration test per shape — `src/req-170.test.js`, finished/active/draft × `snapshot.routineId`/`snapshot.sessionId`,
   each asserting load keeps the id and delete archives. Red on main (model.js change removed, `node --test src/req-170.test.js`):
   `# pass 7  # fail 12` — all 6 shapes × {load, delete} fail; the 7 passing are controls (top-level
   routineId/sessionId wins ×3 each, and "no routine anywhere stays undefined"). On the branch: `# pass 19  # fail 0`.
2. Old keys survive — `node --test src/req-164.test.js`: `ok 1 - req-164 — migrate, load, save round-trip and finish
   deep-equal main (b78e73d)`, `# pass 12 # fail 0`. db.json count, main's `migrateState` vs the branch's
   (scratch script, both legacy=true/false):
   ```
   legacy=true: records=13 routineId changed=0 deepEqual(whole state)=true
   legacy=false: records=13 routineId changed=0 deepEqual(whole state)=true
   db.json records with no top-level routineId/sessionId but a snapshot one: 0
   ```
3. Delete archives in the snapshot-only case — test above, plus `./plan qa <ref> --seed` with db.json + routine
   "Snap Probe" (`r-snap-probe`, no slot) named only by a finished workout's `snapshot.routineId`:
   - **req-170 (ed161b4):** on load `routineId: "r-snap-probe"`; Delete confirm read "Snap Probe has past workouts and
     will be archived (kept in your history)."; after it, stored `archivedAt: "2026-09-25T17:14:54.928Z"`.
   - **main (9a993c1), same seed:** confirm read "Delete Snap Probe?"; after it, routine `GONE`, workout `routineId` undefined.
4. `./check --smoke`: `smoke: green — 11 steps` / `check: green — lint, skills, no import cycles, 72 test file(s), the build, and the smoke test all passed.`
   Full suite `node --test`: `# pass 1079 # fail 0`.

## Workflow

- **Test edit (called out):** `src/req-169.test.js:52` pinned the old behaviour (`w2.routineId === undefined`). Renamed
  to state the rule and inverted: w2 now keeps `r-snap`, and deleting it archives. Header/fixture comments updated to match.
  This was the reviewer nit asked for; it would have gone red anyway.
- No scope added. Nothing for DECISIONS beyond DEC-090.
