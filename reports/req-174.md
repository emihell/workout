# req-174 — an empty home says "Create your first routine"

## Technical

- `src/views/Today.jsx` (first-run block, `isFirstRun(store)` gate unchanged): subtitle `"No data. Import, or start empty."` → **`"Nothing here yet."`**; added `<NavLink to="/routines/new" look="primary" block>Create your first routine</NavLink>` above the existing Import `FileButton` (untouched, still the secondary default).
- `src/req-174.test.js` (new): source-locks the block (the button appears once, only inside the `isFirstRun` block; link target/look; Import follows it with no variant; `importWithBackup` call intact) and asserts `isFirstRun` for the three seed shapes (empty → true, history-only → false, routine-only → false).

Browser receipts (`node scripts/qa.mjs start <ref> --seed <file>`, puppeteer 390x844, dialogs stubbed). Seeds are `emptyState()`, `db.json` with `routines: []` (13 workouts), and `db.json` with `workouts: []` (3 routines):

```
[branch-empty] home text: Today | Nothing here yet. | Create your first routine | Import | Routines | › | Schedule | › | History | › | Settings | › | Workout
[branch-empty] first-routine button: {"tag":"A","href":"#/routines/new","cls":"ui-btn ui-btn--primary ui-btn--block","w":342}
[branch-empty] Import button: {"cls":"ui-btn ui-btn--secondary","w":84}
[branch-empty] after click: hash=#/routines/new text: ‹ Back | Add routine | Name | Focus | ... | Cancel | Next | Workout
[branch-empty] import: Replace clicked=true stored v9={"routines":3,"workouts":13,"exercises":22}
[branch-history] home text: Good evening | Nothing scheduled. | ... | Start new workout | ...        first-routine button: null
[branch-routine] home text: Good evening | Thu, Oct 1 | Lower Body — Machines | Start | ... | No history yet. | History   first-routine button: null
[main-empty]     home text: Today | No data. Import, or start empty. | Import | Routines | ...
[main-history]   home text: identical to branch-history
[main-routine]   home text: identical to branch-routine
```

`./check`: `check: green — lint, skills, no import cycles, 75 test file(s), and the build all passed.` `./check --smoke`: `check: green — lint, skills, no import cycles, 75 test file(s), the build, and the smoke test all passed.` (smoke 12 steps).

## Workflow

- **Layout choice:** a full-width primary NavLink stacked above the Import button, not an `Actions` row. Two reasons. `req-122.test.js:82` counts `<Actions` call sites (15), and it treats `forward={<NavLink` as an inverted row, so an Actions row would have meant editing that test or getting past its regex with a line break. It also matches the pattern Today already uses (`TodayEmpty`'s `NavLink look="primary" block`, DEC-040). No existing test was edited.
- **Subtitle `(unconfirmed)`:** "Nothing here yet." (the spec's example). It goes on Emilio's list.
- **Seen, not fixed:** the primary button and Import sit close together (the Screen gap is small). This was already true of the Title→Import spacing on main. No CSS change, since it's out of scope.
