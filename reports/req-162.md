# req-162 — Skip exercise flashed "Not found." for one render

Branch `req-162` (`20e61e0`, then this report). Lane: bug.

## Technical

- **`src/views/workout/item.jsx` `WorkoutItemLog`:** a marked-done item now returns `null` while its replace-redirect
  effect is pending, where it used to return `<MissingItem />`. An unknown item id with an active workout still returns
  `<MissingItem />`, and no active workout still goes to `NotInWorkout`. Only the log route had this branch.
- **`src/req-162.test.js`** (req-156 harness) mounts the real `WorkoutItemLog` with a store stand-in.
  - Marked-done: renders `''` and the hash becomes `#/workout/r` (the redirect still runs).
  - Unknown id: renders "Not found.".
- **`scripts/smoke.mjs`:** the page's "Not found." tripwire now fails **the step it fires in**, whichever step that is.
  It was a `note:` before, and a hard failure only during the Back step. The `notes` plumbing is removed.
- **`./check`** runs `scripts/check_skills.py` as step 1b, after lint. It takes 0.13 s
  (`time python3 scripts/check_skills.py`). The green line now says "lint, skills, …". Nothing parses it (grep of
  `plan`, `scripts/`, `.githooks/`, `.github/`: no hits).

### Receipts

Red on main (the render test before the fix):
```
    not ok 1 - renders nothing (no "Not found.") and redirects to the overview
      actual: '‹ BackNot found.'
# pass 1
# fail 1
```
After the fix it's `# pass 2 # fail 0`.

Smoke's hard check, red on a main build (`node scripts/smoke.mjs --ref main`, this branch's script):
```
  ok 5 — back to the exercise list (‹ Exercises)
smoke: RED at step "open Lat Pulldown and Skip exercise" — "Not found." rendered (even one render counts) at #/workout/sess-upper
smoke: at http://127.0.0.1:61865/#/workout/sess-upper, the screen reads:
  | ‹ Back
  | Not found.
exit=1
```

`./check --smoke` on the branch (6.7 s; plain `./check` 2.0 s):
```
  ok 6 — open Lat Pulldown and Skip exercise
  …
  ok 11 — History shows the workout (reopen the app → History → the row)
smoke: green — 11 steps (build 0.3s, browser 4.4s)
check: green — lint, skills, 64 test file(s), the build, and the smoke test all passed.
```
The run printed no `note:` line.

The skills step fires (L-040: a throwaway commit with `§Requirement lanes` → `§Renamed lanes` in qa-branch, then
`git archive` and `./check` in a temp dir):
```
check_skills: qa-branch       FAIL
    §Renamed lanes is not a heading in the rule files
check: RED — the skills check (scripts/check_skills.py) failed; refusing.
exit=1
```

## Workflow

No deviations. No test was edited. The browser re-run of the flow is the smoke test itself, which clicks Skip exercise
for real.
