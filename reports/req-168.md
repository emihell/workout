# req-168 — backlog sweep (the last low-priority follow-ups)

Branch `req-168` (`b4adba8` work commit, then this report). Lane: bug. The reviewer fires (`state-reducers.js`). No
stored data is rewritten. **3 of 6 items built or tested; 3 dropped because they don't reproduce on main (L-042).**

## Technical

Each item was reproduced on **main** first (`bdb32bc`), with its real code:

| # | on main | result |
|---|---|---|
| 1 | **reproduces:** `exercise referenced only by a draft, after delete: HARD-DELETED` / `routine … HARD-DELETED` | fixed |
| 2 | the behaviour is right; the component tests were missing | tests added (pins) |
| 3 | the behaviour is right (one `now` per Import; quota → retry works); the tests were missing | tests added (pins) |
| 4 | **doesn't reproduce.** main's real hook, in a sandbox: 5 empty `req-*` branches created by `branch` / `checkout -b` / `switch -c` / `worktree add -b` / `branch X HEAD` at main's unpushed tip → `exit=0`; a real FF of a req branch with a commit → `exit=1 Refusing…` | dropped. req-129's `never_moved` fixed it after the 2026-09-23 incident |
| 5 | **doesn't reproduce.** main's `find_worktrees` in a copy with `code/.claude/worktrees/agent-req120` on `req-120` → `CODE_DIR=…/code (on main)` | dropped. req-129's `worktree_is_nested` |
| 6 | **doesn't reproduce.** `scripts/screenshot.mjs` on main already has `--click "<text>"` (repeatable) and `--scroll-bottom` (req-129, `9bdf8ac`) | dropped. BACKLOG is stale (`shot` isn't redundant: it shoots named screens; `plan qa` + the smoke drive flows) |

1. **Drafts are references.** `routineDeletionImpact` and `exerciseDeletionImpact` count legacy `draftWorkouts` in
   `hasHistory`: a draft's routineId, its sets and its snapshot items. `hasHistory` is what both the delete
   reducers and the confirm (`deletionConfirmHead`) read, so they agree ("…will be archived").
   - `exerciseInActiveWorkout`'s logged-sets branch is **kept and tested**, not removed. Migration rebuilds a legacy
     in-progress snapshot from its sets (measured), and live logging always logs against an item. So only a
     hand-edited import can reach it, and dropping a reference check would risk DESIGN §3 for no gain.
2. **Render tests (req-156 harness).**
   - History Add set: type kg/reps, then Cancel → no `updateWorkout` call, and the workouts are byte-identical.
   - item.jsx, through the **real** StoreProvider: make Plank timed with 2 sets, Start, log set 1 at 50 s, re-render,
     tap Previous → the Duration field reads `50` (not the 60 s target).
3. **`src/req-168.import.test.js`.**
   - (a) With v9 + v8 + v7 present and v8 already kept by an earlier Import, the new v9 and v7 copies share **one ISO**,
     and the reused copy is untouched.
   - (b) Quota → `KEEP_NO_SPACE_MESSAGE`, lock kept, writes refused → space freed → retry → both copies, lock
     released, the import saved, and the next load cleans v8 up while the copies stay.

### Receipts
Red on main (both files in a `git archive main` copy): 4 fail, 6 pass.
```
    ok 1 - (a) one Import's new copies share ONE timestamp, even with a reused copy between them
    ok 2 - (b) quota → the storage-full message, lock kept → free space → retry → copies kept, lock released, import saved
    not ok 1 - an exercise whose only reference is a draft (a logged set) → archived: archivedAt set, not removed (was: hard-deleted)
    not ok 2 - …or only a draft's snapshot item → archived too
    not ok 3 - a routine whose only reference is a draft → archived (was: hard-deleted)
    not ok 4 - the confirm names it the same way the delete treats it (both read hasHistory)
    ok 5 - control: unreferenced setup is still hard-deleted
    ok 6 - exerciseInActiveWorkout's logged-sets branch: …still counts
    ok 1 - History's Add set writes nothing before Save: type values, Cancel → the workout is byte-identical
    ok 2 - item.jsx passes a restored duration to the form: log a timed set at 50 s, Previous → the Duration field reads 50
# pass 6
# fail 4
```
On the branch: 10/10, and the full suite is `# pass 1043 # fail 0`.

The pins' teeth, from throwaway commits (L-040), each caught by its test:
```
== mutation: 2a: Add set writes a placeholder on open (the req-117 bug)          → not ok 1 - History's Add set writes nothing …
== mutation: 2b: item.jsx gives the form the target, not the restored seconds    → not ok 2 - item.jsx passes a restored duration …
== mutation: 3a: each copy gets its own timestamp                                → not ok 1 - (a) … (and 2)
== mutation: 3b: a failed copy releases the lock                                 → not ok 2 - (b) quota → …
== mutation: 1: exerciseInActiveWorkout's logged-sets branch removed             → not ok 6 - …logged-sets branch …
```
```
smoke: green — 11 steps (build 0.4s, browser 4.7s)
check: green — lint, skills, no import cycles, 70 test file(s), the build, and the smoke test all passed.
```

## Workflow

- **Dropped per L-042 (the code wins):** items 4, 5 and 6 were fixed by **req-129** (`9bdf8ac`, 2026-09-2x), after
  the incidents BACKLOG cites. The BACKLOG lines (req-120 pre-push, req-120 nested worktree, req-105 shot) should be
  struck.
- **Choice:** item 1 keeps `exerciseInActiveWorkout`'s sets branch (tested) instead of removing it. The reason is
  above.
- **User-visible (small):** deleting setup referenced only by a legacy draft now **archives** it, and the confirm says
  "…will be archived (kept in your history)". Before, it deleted it outright. That is what DESIGN §3 requires.
