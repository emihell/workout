# req-115 — a bad import shows an error instead of blanking the app

Branch `req-115` (from `main` 4aa0e4a). Touches `store.jsx` + `storage.js` → DEC-057: independent reviewer
and DEC-046 backup reminder before merge.

## Technical

- `exchange.js` — new pure `commitBackup(payload, setState)`: runs `applyBackup` first, then
  `setState(() => result.state)`. A throw reaches the caller with `setState` never called.
  `collectionsAreArrays` → `collectionsAreValid`: every element of `exercises`, `routines`, `sessions`,
  `programs`, `workouts`, `plannedWorkouts`, `draftWorkouts`, `schedule.slots` must be a plain object;
  nested `routines[].exercises`, `programs[].sessions[].exercises`, `workouts|draftWorkouts[].sets`,
  `.snapshot` (null or object) `.items`, `plannedWorkouts[].items` must be arrays of plain objects;
  `activeWorkout` null/absent or a plain object passing the workout check. `state` must be a plain object
  (was `typeof 'object'`, so an array passed). Absent fields stay fine (req-39 rule).
- `store.jsx:379` — `applyBackup(payload) { return commitBackup(payload, setState) }`.
- `storage.js` `loadState` — a parsed value that is not a plain object (`"x"`, `[1,2]`, `42`, `null`)
  throws into the existing DEC-032 catch: unreadable latched, saves held, v9 and legacy keys untouched.
- `App.jsx` — outer `ErrorBoundary` around `StoreProvider` (the existing component, unchanged); the inner
  one around `<Screen/>` stays.
- Tests: new `src/safe-import.test.js` (24 tests). No existing test edited.

### Receipts

`./check`: `check: green — lint, 28 test file(s), and the build all passed.` (408 tests, 0 fail)

Unit (`node --test src/safe-import.test.js`): `# tests 24 # pass 24 # fail 0` — analytics export,
`{workouts:[null]}`, `routines[0].exercises:[null]`, `schedule.slots:[null]`, `activeWorkout: 42` (+11 more
shapes) each throw `Not a workout database backup.` with `setState` calls = 0 and state the same object;
valid backup round-trips `deepEqual`; v9 = `"x"`/`[1,2]`/`42`/`null` + v8 → unreadable, v8 present.

Probe against main vs branch source (`scratchpad/main115/probe.mjs`):
```
MAIN   v9="x": unreadable=false v8 present=false v9={"0":"x","schemaVers
MAIN   routines[0].exercises:[null] -> accepted
BRANCH v9="x": unreadable=true v8 present=true v9="x"
BRANCH routines[0].exercises:[null] -> rejected
```
Puppeteer, Settings → Import, seeded v9 device, 390x844 (`scratchpad/import115.mjs`):
```
main   analytics    {"rootChildren":0,"alert":[],"hasNav":false,"storedUnchanged":true,"pageErrors":["Not a workout database backup."]}
main   workoutsNull {"rootChildren":0,...,"pageErrors":["Cannot read properties of null (reading 'routineId')"]}
branch analytics    {"rootChildren":2,"alert":["Not a workout database backup."],"hasNav":true,"title":"Settings","storedUnchanged":true,"pageErrors":[]}
branch workoutsNull {"rootChildren":2,"alert":["Not a workout database backup."],"hasNav":true,"title":"Settings","storedUnchanged":true,"pageErrors":[]}
```

### Dependents

- `Settings.jsx:5` / `Today.jsx:3` import `importWithBackup` from `../import-backup` — unchanged; it calls
  `store.applyBackup`, same signature, still returns `{state, summary}` or throws. Holds
  (`import-backup.test.js` green; puppeteer Settings path above).
- `loadState` — only caller `store.jsx:11` (`useState(loadState)`). Behaviour changes only for a
  non-object parse. All req-06/36/37/55/85 migration tests pass unchanged.
- `unwrapBackup` — only `applyBackup` + `exchange.test.js` (req-39 tests pass unchanged).
- `ErrorBoundary` — now used twice in `App.jsx`; `error-boundary.test.js` green.

## Workflow

- Error text kept as the existing `Not a workout database backup.` rather than the spec's "Not a valid
  backup": it is the pinned message in `exchange.test.js`/`import-backup.test.js` and changing it means editing
  tests. **(unconfirmed)**
- The outer boundary reuses the existing fallback ("Something went wrong on this screen… Your data is saved",
  Reload / Back to Today); above the store there's no nav. **(unconfirmed)** — the Planner already marked this.
- `null` v9 value also counts as unreadable (spec listed `"x"`, `[1,2]`, `42`). **(unconfirmed)**, safe side.
- Added checks the spec didn't list but its rule covers: `sessions`, `programs[].sessions`, `draftWorkouts`,
  `snapshot` itself non-object, `state` as array.
- Follow-up: a stored v9 with `[null]` elements that passes `migrateState` now hits the outer boundary on
  every load (no blank page, but no recovery path either). Load-path validation is out of scope here.
