# req-173 — "No weight entered" note on a weighted set

Branch `req-173` off `7e1de43`.

## Technical

- `src/ui/index.jsx` SetLogForm: when the form is `weighted` and `readKg(weight).empty` (blank or whitespace), a
  `<p className="ui-field-note">No weight entered</p>` renders under the kg/reps row, in the slot the inline kg error
  uses. The submit path is untouched: Complete still logs in one tap, and `liveSetWeight` still saves 0.
  Unweighted forms (bodyweight/cardio) never show it. Unreadable text (`abc`) shows the existing error, not the note.
- `src/ui/ui.css`: `.ui-field-note`, same margins and caption size as `.ui-field-error`, in `--ui-ink-2`, with no bold
  and no `!` marker.
- `src/req-173.test.js` (new, render tests): AC1 note shows/hides/unweighted; one-tap submit; AC2 the real live
  log screen + store, Leg Extension work set with kg cleared → stored set deep-equals a pinned object (`weight: 0`).
  The pinned object came from running the same steps with main's `ui/index.jsx` swapped in. Main produced the same
  JSON: `{"routineItemId":"si-sess-lower-2-ex-leg-extension","exerciseId":"ex-leg-extension","setType":"work","weight":0,"reps":"12","rpe":3,"note":"","targetReps":"12","targetWeight":20}`.

Receipts:
- `node --test src/req-173.test.js` → `# pass 6`, `# fail 0`.
- AC3 [measured] (puppeteer 390x844, `node scripts/qa.mjs start <ref> --seed seed-173.json`, a seed of db.json with
  no workouts and Lower Body's Leg Press at 1 set, no warm-up, no suggested weights). Workout 1 (Thu 2026-09-24):
  Leg Press, blank kg, Complete, Finish, Save. Stored Leg Press set on both refs: `weight: 0`. Workout 2
  (Thu 2026-10-01), Leg Press kg box: **main `""`**, **branch `""`** (the note shows on the branch). Main has no
  invented "0" prefill.
- `./check --smoke` → `check: green — lint, skills, no import cycles, 74 test file(s), the build, and the smoke test all passed.`

Choices the spec left open: the note sits under the whole kg/reps row, where the kg error sits, rather than
inside the kg column. It has no `role`/`aria-live` because it is a quiet hint, not an alert.

## Workflow

- "History set editor if it shares the form": it doesn't. History and the active set edit use `SetEditForm`
  (`views/set-edit.jsx`), not SetLogForm, so I left both unchanged. History keeps a blank kg as `''`, not 0.
  If the active set edit (blank → 0) should get the note too, that needs its own req.
- No test edits. No schema/storage change. Not merged, not pushed.
