# req-104 — exercise screens trim

## Technical

- `src/views/workout/item.jsx` — `WorkoutItemDone`: removed the `Previous` SectionHeader, its
  list and its "None." fallback, and the `lastSetsForExercise` call that fed them. Today, Add set and
  the exercise header are unchanged. `lastSetsForExercise` is still imported and used by
  `WorkoutItemLive` (log-screen prefills, item.jsx:149).
- `src/ui/ui.css` — `.ui-exercise-head .ui-title` now sets `font-size: var(--ui-text-section)`
  (22px, was the 32px `--ui-text-title`). `.ui-exercise-head` is used only by `ExerciseTitle`, so
  the shared `Title` / `.ui-title` stays 32px on every other screen. Still bold, still the `h1`.
- `src/views/workout/item.test.js` (new) — static-source tests, same approach as
  `views/shared.test.js`: the done view has no Previous header or `lastSetsForExercise(` call and
  exactly one "None." (Today's); the log screen still calls `lastSetsForExercise`; the size is
  scoped to the exercise head and `.ui-title` keeps `--ui-text-title`.

Receipts:
- `node --test src/views/workout/item.test.js` → `# tests 3 / # pass 3 / # fail 0`
- `./check` → `check: green — lint, 22 test file(s), and the build all passed.`
- Screenshots (390x844, built app, seeded active workout from `src/db.json`, Upper Body, Chest Press
  marked done with one logged set):
  - done view with history: before shows Previous + 4 rows; after shows no Previous header or rows.
  - done view with `workouts: []`: no Previous header and no stray "None.".
  - log view (Lat Pulldown): title visibly smaller (22px vs 32px); "Add note" stays on the same row,
    right-aligned, not wrapped; meta line `WU set · 1/4` directly below.
  - Today (`/`): "Good morning" / date titles still at full title size.

Choice the spec left open: the size is `--ui-text-section` (22px), an existing token, so the exercise
name matches the SectionHeader scale instead of adding a new size.

## Workflow

No deviations; stayed in scope. A test file was added, and no existing test was edited.
Side effect: on the done view, the exercise name (22px/700) is now the same size as the "Today"
section header (22px/600). This is expected from the chosen token, and worth a look when judging the feel.
