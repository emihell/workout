# req-142 — remove RepDB completely

Branch `req-142` is off `main` at adb0a20. It is not merged.

## Ready to look at

1. **What it does.** Removes the last of RepDB:
   - the Settings credit (`src/views/credits.jsx`, and `ExternalLink`, which had no other user);
   - `scripts/library-gap.mjs` and `scripts/originality.mjs`;
   - the local `.vendor-cache/` folder and its `.gitignore` line.

   The forward-looking comments ("RepDB returns for pictures, req-131") are reworded as history, including one line in
   `PROVENANCE.md`. Exercises added from RepDB before req-139 (`repdb-*` ids) still load, back up, and resolve (to null,
   or by name) without errors.
2. **What to test** (yes/no):
   1. Is `./check` green?
   2. Does Settings show no RepDB line (Export / Import / Components / Feedback notes only)?
   3. Does an old backup with a `repdb-*` exercise still import and show that exercise?
   4. Is the main chunk 341.64 kB (−0.30)?
3. **Not verified by me.** A real device with a pre-req-139 `repdb-*` exercise in its stored data (covered by unit
   tests only).

## Technical

**Removed:**
- `src/views/credits.jsx` (`RepdbCredit`, `REPDB_CREDIT`), and its import and use in `Settings.jsx`.
- `ExternalLink` in `src/ui/index.jsx`: its only user was the credit, so per the spec it's deleted.
- `scripts/library-gap.mjs` and `scripts/originality.mjs`. No npm scripts referenced them.
- `.vendor-cache/`, deleted locally (it was never in git), and its `.gitignore` block.
- Comments reworded to history: `exerciseCatalog.js:5-6`, `exerciseLibrary.js:10-11`, `PROVENANCE.md` §What is not here.

**No dead code helpers were left to remove.** `fromRepdbItem`, `REPDB_URL` and the merge already went in req-139.
There's also **no special `repdb-` branch in the code**. A stored `repdb-*` libraryId isn't in the index, so
`libraryEntryFor` falls through to the name, like any unknown id. That fallthrough is the "handling" that is kept, and
it's now covered by tests.

**`git grep -in repdb -- src scripts`** (excluding `exercises.json`, which has 0 matches):
```
src/exerciseCatalog.js:5:     History: RepDB was used until req-142; nothing        [history]
src/exerciseCatalog.js:6:     … Exercises added from it earlier (repdb-* ids) are untouched and resolve by name.   [history + stored handling]
src/exerciseCatalog.js:165:   // req-130 — every catalog item is a library entry since req-139 (no RepDB hits).   [history]
src/exerciseCatalog.test.js:87:  // req-139 (sanctioned edit) — … left with the RepDB merge.   [history]
src/exerciseLibrary.js:10-11: Nothing from RepDB goes in the library … History: RepDB was used until req-142   [guard note + history]
src/exerciseLibrary.test.js:2,214,270,279,303: comments on earlier edits   [history]
src/exerciseLibrary.test.js:244-246: describe('no RepDB data in the library') … doesNotMatch(LIBRARY_TEXT, /repdb/i)   [KEPT GUARD]
src/exerciseLibrary.test.js:271,275: loadExerciseCatalog … every id !startsWith('repdb-')   [KEPT GUARD]
src/library/PROVENANCE.md:30,36-37: never RepDB's … RepDB was used until req-142   [history]
src/library/text.js:6, src/library/triage.js:2, src/req-133.test.js:248: "never/no RepDB"   [history]
src/req-139.test.js:222-231: Search/Settings show no RepDB credit; the catalog has no RepDB fetch   [KEPT GUARDS]
```
Plus `src/req-142.test.js` (new, below). `grep -ci repdb src/library/exercises.json` → **0**. `ls .vendor-cache` →
`No such file or directory`.

**Settings screenshot** (`npm run shot -- /settings --build --full --out screenshots/req-142-settings.png`; the folder is
gitignored): the page shows Settings → Assistant prompt, Export / Export analytics / Import, Components, Feedback notes,
and the dock. There's no credit line.

**Receipts.**
```
$ ./check
check: green — lint, 54 test file(s), and the build all passed.
$ node --test src/req-142.test.js   →  # pass 5  # fail 0
L-026 main chunk: 341.94 kB (main @adb0a20) → 341.64 kB (credit + ExternalLink gone).
```
`src/req-142.test.js`:
- a stored `repdb-x` exercise (id and libraryId) survives `migrateState` unchanged;
- `libraryEntryFor` returns null for an unmatched name, and resolves by name when the name matches (unchanged);
- it never matches a library Search row, and round-trips through `buildBackup` → `applyBackup`;
- the credit file, both scripts and the cache folder are gone, and neither `.gitignore` nor `package.json` mentions them.

**Test edits (sanctioned: tests of removed pieces):**
- `req-139.test.js:221-226`: "Settings keeps the credit" became "Settings has no credit either"
  (`doesNotMatch /RepdbCredit|repdb/i`). The Search half is unchanged.

No test existed for the scripts, so none was deleted.

## Workflow

1. **`CLAUDE.md:81`** still cites "a RepDB cache (DEC-066 §3)" as its example of the gitignored-data carve-out.
   Planning maintains `CLAUDE.md`, so I left it for you to reword or keep as history.
2. **The originality check is gone with the script.** Future authored library text has no automated RepDB comparison,
   which is intended now that RepDB is removed (DEC-069). req-141's fresh scan is the next content check.
3. Nothing else deviated.
