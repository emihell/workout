# req-128 — small fixes bundle (report)

Branch `req-128`, from `main` @ `5649921`. No stored-data change (no schema, migration or bulk write).

## Technical

1. **History detail labels** — `historyGroupMeta(snapshotItem, setCount)` (new, `src/views/history/helpers.js`)
   builds the row meta with `roleTag` (req-93): main/absent role → nothing; warm-up/finisher/cardio → tag; then
   `WU set`, then `N sets`. `detail.jsx` row uses it; the per-exercise screen's sub line (`HistoryWorkoutExercise`)
   switched `roleLabel` → `roleTag` too, so "Main" is gone from both history screens. helpers.js imports now carry
   `.js` (`../../ids.js`, `../../schedule.js`) so `node --test` can import it; Vite resolves either.
2. **Summary prior** — `summaryPriorWorkout(active, workouts, routines)` (new, `storage.js`) =
   `previousSameRoutineWorkouts(...).find(anythingLogged) ?? null`. `auto-complete.jsx:41` calls it instead of
   `previousSameRoutineWorkout`. `workoutSummaryStats` unchanged (null prior → no deltas).
3. **historyPrescription** — among snapshot items of the exercise, prefers the first that is not
   `isAddedMidWorkout`, else the first; `restSec` and `notes` both come from it (`storage.js` ~485).
4. **Wake-lock** — pure `wakeLockWanted(activeWorkout, hash)` in `wake-lock.js`: active AND the hash path starts
   with `/workout/`. `WakeLock` keeps its own `hashchange` subscription (not `useHashRoute`, which also records a
   screen view). Leaving to Settings releases; returning re-acquires via the existing effect.
5. **Dead code** — removed `nextDateForSlot`, `nextOccurrence` (`schedule.js`), `parseTargets` (`ids.js`).

Tests — `src/req-128.test.js` (items 1–4, 10 cases) + 2 new cases in `wake-lock.test.js`:
```
ok 1 - req-128 1: History detail row meta follows the req-93 rule
ok 2 - req-128 2: the summary compares against the latest prior with anything logged
ok 3 - req-128 3: historyPrescription reads rest/notes from the routine item, not a replacement
ok 4 - req-128 4: wake-lock predicate — active workout AND a /workout/… route
    ok 8 - req-128: leaving the workout route releases; coming back re-acquires
    ok 9 - req-128: an active workout on a non-workout route never requests
# pass 20   # fail 0     (node --test src/req-128.test.js src/wake-lock.test.js)
```
Item 3's fixture has the replacement (restSec 0, notes '') BEFORE the routine item (120, 'Pause at top'); old code
returned 0/''. Item 5 is static: `grep -rn "nextOccurrence\|nextDateForSlot\|parseTargets" src` → no output, exit 1.

`./check`: `check: green — lint, 42 test file(s), and the build all passed.`

Browser (puppeteer, 390x844, seeded throwaway store on a dev port): History detail rows read
`Jump rope — WU routine · 1 set`, `Bench press — WU set · 3 sets`, `Row — 1 set`, `Plank — Finisher · 1 set`.

## Workflow

- **Deleted tests** (tests of deleted code): `schedule.test.js` — `describe('nextOccurrence')` (both cases: "picks
  the next uncovered scheduled day", "skips a day already done early") and "resolves a concrete date from a
  recurring slot"; plus the now-unused `routines` / `schedule` fixtures. `parseTargets` had no tests.
- **Edited test**: `wake-lock.test.js` `beforeEach` now stubs `window` with hash `#/workout/r1` (restored in
  `afterEach`) — the lock now needs a workout route, so existing cases run on one; their expectations unchanged.
- **Outside the listed scope**: `auto-complete.jsx` (one-line swap to `summaryPriorWorkout`) and `helpers.js`
  (new pure helper — tests can't import `.jsx`). `beat-last-time.js` untouched (spec: out of scope for item 3).
- **(unconfirmed)** History rows keep the plain text treatment — main is not bolded and the tag is not the muted
  `ui-role-tag` span as in the in-workout list; wording is `roleTag`'s (`WU routine`, `Finisher`, `Cardio`).
- **(unconfirmed)** Wake-lock "workout route" = any `#/workout/…` path, which includes preview/setup/finish screens.
- **Follow-up**: `previousSameRoutineWorkout` now has no production caller (tests only) — candidate dead code.
