# req-177 — phone polish (report)

Branch `req-177`, on `origin/main` f04a420. Display only; no stored value changes; `src/workout-log.js` untouched.

## Technical

- **Effort "Max"** — `src/ids.js` RPE_OPTIONS + `rpeLabel(>=5)`, `src/ui/index.jsx` SetLogForm default `effortOptions`
  (+ comment), `src/ui/Showcase.jsx`. Value stays `5`.
- **Finish list** — `src/finish-unfinished.js`: `finishSkippedByItem` unchanged. `finishSkippedLine` now covers only a
  started (partly-logged) item and returns `null` when only a warm-up is left; `finishSkippedLines` = partly-logged lines
  in workout order, then one `Not started, saved as skipped: A, B, C.` line (workout order) when any item is untouched.
- **"1 set"** — `views/workout/finish.jsx` summary and `views/history/detail.jsx` summary, inline ternary (the codebase's
  existing pattern, e.g. `history/helpers.js:22`).
- **Home gap** — `views/Today.jsx` first-run block wraps the two actions in `.ui-first-run-actions` (`ui.css`): flex column,
  `gap: var(--ui-s2)` — the token `.ui-workout-end` and `.ui-actions` use; `align-items: flex-start` keeps Import its own width.
- New `src/req-177.test.js` (AC1, AC3, AC4, AC5).

Receipts:
- `./check` → `check: green — lint, skills, no import cycles, 78 test file(s), and the build all passed.` (1136 pass, 0 fail)
- AC1 browser (390×844 @2x, qa build 447e08c): effort segments `[Easy 44, Moderate 44, Hard 44, Max 44]` px high;
  after picking Max + Complete on Chest Press, stored active sets
  `[{"ex":"ex-chest-press","t":"wu","rpe":null},{"ex":"ex-chest-press","t":"work","rpe":5}]`. Main side: req-175's
  EXPECTED_MAIN (pinned from main, picks the value-5 option) still passes with rpe 5.
- AC2 browser, Upper Body, 1 Chest Press work set then Finish: lines
  `Chest Press: 1 of 3 sets. The other 2 will be saved as skipped.` /
  `Not started, saved as skipped: Rowing, Lat Pulldown, Shoulder Press, Biceps Curl, Triceps Press, Ab Machine, Plank, Push-Ups.`;
  Feel `getBoundingClientRect().top` = **336.4** (< 844); Save top 559.4; page scrollHeight 844 (no scroll).
- Home: Create bottom 158.4 → Import top 166.4 = **8 px** gap (was ~4).

## Workflow

- Test edits (wording only, each marked in place):
  - `ids.test.js` — title + `rpeLabel(5)` → `'Max'`.
  - `req-175.test.js` — two test titles; `effort5` → `'Max'`; History match `/Couldn't finish/` → `/ · Max\b/`.
  - `req-116.test.js:238` — source regex now matches the singularizing template (still asserts `loggedSetCount(workout)`,
    still `doesNotMatch` `sets.length`).
  - `req-176.test.js` — expected lines reworded (not-started → the collapsed line; warm-up no longer named; Row, only its
    warm-up left, now has no line); the warm-up test title; the screen test's `lines.length === items.length` moved to
    `finishSkippedByItem(...).length === items.length` plus `lines.length === 2`; `/not started\./` → the new prefix.
    All `assertMatchesSave` (AC3's mechanism check) unchanged.
- Left open by the spec, chosen here: (1) the "Not started" line goes **after** the partly-logged lines; (2) an exercise
  with only its warm-up unlogged gets **no line** (its warm-up is still saved as skipped); (3) an exercise with only its
  warm-up logged ("Dip: 0 of 2 sets. 2 sets will be saved as skipped.") counts as partly logged; (4) names are
  `exerciseName`, so role suffixes shown on the overview ("Rowing Warm-up", "Push-Ups Finisher") are not in the line.
