# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# req-89 — headless pre-merge screenshots (dev-only, never committed)
screenshots/

# local, per-machine Claude settings
.claude/settings.local.json
# ephemeral build-agent git worktrees (isolation: worktree)
.claude/worktrees/

# req-140 / DEC-066 — licensed third-party data (RepDB cache, gap table): in the folder, never in git.
.vendor-cache/
